<%= form_for @trip, url: trip_index_path do |f| %>

<%= f.label :name, "What's the title of your trip?" %>
<%= f.text_field :name, placeholder: "ex. Canadian Roadtrip 2020" %>
<%= f.submit "Create title"%>

<% end %>
